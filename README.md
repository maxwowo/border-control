# Border Control

A simple utility for adding borders to images.

## Installation

```bash
# With Pipenv
$ pipenv install
```

## Usage

```bash
$ pipenv shell
$ python main.py --src ~/some/path/ --dst ~/some/other/path/
```
